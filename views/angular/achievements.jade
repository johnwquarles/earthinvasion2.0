extends ./partials/_layout.jade

block content
  .achievements(ng-controller='achievementCtrl as ctrl')
    .achievementWrapper(ng-repeat='achievement in ctrl.getAchievements()')
      .achievementIcon
        .textWrap
          p.title {{achievement.title}}
          p.description {{achievement.description}}
